# Open a terminal in packages directory

A very simple plugin that opens a terminal in the packages directory

## Settings

 
 * `terminal-emulator`: To specify a command line interpreter emulator on linux distros.